# AIFlow Desktop / Server example configuration
#
# 1) Copy to .env
# 2) Fill your own keys
# 3) Start dependencies (docker-compose.aiflow.yml) and run aiflow-desktop.exe

# Server
ADDR=:9999
ADMIN_ADDR=127.0.0.1:6060

# Database (Postgres)
DB_DSN=postgres://days:days@localhost:5432/days?sslmode=disable

# Redis
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# Rate limit
RATELIMIT_ENABLED=true

# AIFlow
AIFLOW_ENABLED=true
DEEPSEEK_API_KEY=
DEEPSEEK_BASEURL=https://api.siliconflow.cn/v1
DEEPSEEK_MODEL=deepseek-ai/DeepSeek-V3.2

# Optional: Web search tool
SERPAPI_KEY=

# Optional: Knowledge base / embeddings (enables memory_query tool backend + KB APIs)
EMBEDDING_API_KEY=
EMBEDDING_BASEURL=https://api.siliconflow.cn/v1
EMBEDDING_MODEL=BAAI/bge-m3

# Timeouts / limits
AIFLOW_AGENT_TIMEOUT=120s
AIFLOW_TOOL_TIMEOUT=20s
AIFLOW_OBSERVATION_MAX_BYTES=8192

# Optional: cost estimation
# AIFLOW_COST_PER_1K_TOKENS=0.0

# Tracing (optional; if you enable, provide OTLP receiver like Jaeger/Tempo/Collector)
TRACING_ENABLED=false
OTLP_GRPC_ENDPOINT=127.0.0.1:4317
OTLP_SERVICE_NAME=aiflow

